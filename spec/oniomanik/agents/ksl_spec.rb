require 'spec_helper'

describe Oniomanik::Agents::Ksl do
  it 'has a version number' do
    expect(Oniomanik::Agents::Ksl::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
